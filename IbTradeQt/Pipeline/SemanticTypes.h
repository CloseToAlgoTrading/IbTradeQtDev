#ifndef PIPELINE_SEMANTICTYPES_H
#define PIPELINE_SEMANTICTYPES_H

#include "../Strategies/Generic/UnifiedModelData.h"

namespace Pipeline {

/// Canonical semantic handoff between pipeline stages (legacy-compatible).
using ModelDataList = DataListPtr;

} // namespace Pipeline

#endif // PIPELINE_SEMANTICTYPES_H
